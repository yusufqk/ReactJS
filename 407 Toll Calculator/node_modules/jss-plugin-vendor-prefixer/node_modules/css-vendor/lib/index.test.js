'use strict';

var _caniuseSupport = require('caniuse-support');

var msg = 'Detected browser: ' + _caniuseSupport.currentBrowser.id + ' ' + _caniuseSupport.currentBrowser.version;
console.log(msg); // eslint-disable-line no-console